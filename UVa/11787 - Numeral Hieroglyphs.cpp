#include<stdio.h>
#include<string.h>
int main()
{
    int n, B, U, S, P, F, T, M,i,len,N;
    char num[600];
    scanf("%d",&t);
    while(t--)
    {
        scanf("%s",&num);
        len=strlen(num);
        B = U = S = P = F = T = M = 0;

    }
    return 0;
}
