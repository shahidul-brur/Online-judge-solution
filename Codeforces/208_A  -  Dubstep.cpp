#include <bits/stdc++.h>
using namespace std;

int main()
{
    int l, i, j, l1;
    string a, b;
    while(cin>>a)
    {
        l = a.length();
        int f=1;
        for(size_t i = a.find("WUB");i!=string::npos;a.replace(i, 3, " "), i=a.find("WUB"));
        cout << a << "\n";
    }
    return 0;
}
